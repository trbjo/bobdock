[Unit]
Description=BobDock
After=graphical-session.target

[Service]
Type=exec
ExecStart=bobdock

Environment=GSK_RENDERER=gl # alternative renderers: gl, ngl, vulkan
Environment=GDK_BACKEND=wayland

[Install]
WantedBy=graphical-session.target
